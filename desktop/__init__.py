"""PySide6 desktop application — the end-user product.

Views only. Every calculation lives in ``app/`` and is shared with the
development web interface; nothing here decides anything about a voyage.
"""
