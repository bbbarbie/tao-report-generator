# TAO 報表產生器 — 第一次使用說明

這份說明只需要看一次。
照著做完之後，以後每個月只要**點兩下圖示**就可以了。

不需要懂電腦，也不需要輸入任何指令。

---

## 第一步：先裝一個叫 Python 的東西

電腦要先有 Python，這個程式才能跑。**只需要裝這一次。**

1. 打開瀏覽器（Edge 或 Chrome 都可以）
2. 在網址列輸入：**python.org/downloads**
3. 點畫面中間那個**黃色或藍色的大按鈕**，上面寫著 `Download Python`
4. 等它下載完，**打開下載好的檔案**
5. ⚠️ **這一步最重要** ⚠️
   安裝畫面**最下面**有一個小方格，寫著：

   > `Add python.exe to PATH`

   **請先把這個小方格打勾**，再做下一步。
   （如果忘記打勾，程式會打不開，要重裝一次。）
6. 打勾之後，點 **`Install Now`**
7. 等它跑完，看到 `Setup was successful` 就可以按 **`Close`** 關掉

✅ 第一步完成了。

---

## 第二步：打開 TAO 報表產生器

1. 打開放這個程式的資料夾
2. 找到這個檔案，點兩下：

   > **TAO Report Generator**

3. 第一次會跳出一個視窗說「正在第一次安裝，請稍候」

   👉 **請耐心等 3 到 5 分鐘。**
   👉 這段時間畫面可能看起來沒動靜，這是正常的。
   👉 **不要重複點**，也不要關掉電腦。

4. 等它跳出「安裝完成！」，**瀏覽器會自己打開**，就可以用了

✅ 第二步完成了。以後就不用再等了。

---

## 以後每個月怎麼用

點兩下 **TAO Report Generator** → 瀏覽器自己打開 → 就這樣。

在畫面上依序做四件事：

| 順序 | 畫面上寫 | 您要做的事 |
| --- | --- | --- |
| 1 | Daily files | 選擇放每日船舶動態表的資料夾 |
| 2 | Month | 選要做哪一個月 |
| 3 | Process | 按下去，等它算完 |
| 4 | Download | 把做好的報表存下來 |

如果中間出現 **Needs Review**，代表有幾筆資料程式不敢自己決定，
請看下面「如果出現 Needs Review」那一段。

---

## 如果出現 Needs Review（需要您確認）

這**不是錯誤**。是程式遇到不確定的地方，寧可問您，也不要自己亂猜。

畫面上每一筆都會告訴您：

* 是哪一條船、哪一個航次
* 相關的時間
* 到底哪裡不確定
* 有哪幾個選項可以選
* **程式建議選哪一個，以及為什麼**

如果不確定怎麼選，**直接用程式建議的那個**就可以。
選完按 `Save decision`。

其中「這條船是哪一家公司的」這種問題，**回答一次就好**，
以後每個月程式都會自己記得。

> 就算一筆都不回答，還是可以直接下載報表。
> 沒確認的那幾列會用**淡橘色**標起來，方便您之後再看。

---

## 報表上的顏色代表什麼

| 顏色 | 意思 |
| --- | --- |
| 沒有顏色 | 程式自己算好的，沒有問題 |
| 淡綠色 | 您確認過的 |
| 淡橘色 | 還有問題沒確認 |

---

## 遇到問題怎麼辦

### 點了兩下，什麼都沒發生

第一次使用要等 3 到 5 分鐘，請先耐心等一下。
如果等超過 10 分鐘還是沒反應，重新點一次。

### 跳出視窗說「需要先安裝 Python」

代表第一步沒做完，或是**忘記勾選 `Add python.exe to PATH`**。
請回到第一步重做一次，這次記得打勾。

### 跳出視窗說「安裝沒有成功」

通常是網路沒連上。
確認一下電腦可以正常上網，然後重新點一次。

### 想關掉程式

點兩下 **Stop TAO Report Generator** 就可以。

其實不關也沒關係，放著不會有問題。
下次再點 **TAO Report Generator**，它會直接把畫面打開，不會重開一次。

---

## 兩件請放心的事

**您的資料不會外流。**
所有的 Excel 檔案和您做過的確認，全部都留在這台電腦裡，
不會上傳到網路上任何地方。

**不會弄壞您原本的檔案。**
程式只會「讀」每日船舶動態表，不會改動它們。
做好的報表會另外存成新檔案。

---

## 給協助安裝的人

* 程式進入點是 `TAO Report Generator.vbs`，它會隱藏視窗執行 `launcher\start.bat`
* 需要 Python 3.10 以上；第一次執行會自動在專案資料夾內準備好執行環境
* 服務只綁定 `127.0.0.1`，區域網路上的其他電腦連不進來
* 使用者的確認紀錄存在 `config/decisions.json`
* 完整說明見 `README.md`
